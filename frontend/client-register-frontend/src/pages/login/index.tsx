import { LoginContainer } from "../../components/form-login-container";
import { RegisterContainer } from "./styles";

export const Login = () => {
  return (
    <RegisterContainer>
      <LoginContainer></LoginContainer>
    </RegisterContainer>
  );
};
